# payments-service

Internal payments API used by checkout and billing workers. Exposes charge creation and lookup behind JWT bearer auth.

This repository is a **demo fixture** for the Fleet CVE Remediation exercise. It is not a production service.
